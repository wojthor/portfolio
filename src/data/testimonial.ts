/** Featured client testimonial — portrait in `public/image.png` */
export const featuredTestimonialAuthor = {
  fullName: "Wiktor Szyszkowski",
  avatarSrc: "/image.png",
  /** `id` on the matching portfolio entry (`scroll-mt` on the article) */
  linkedProjectAnchorId: "wiktor-landing",
} as const;
